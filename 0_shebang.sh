#!/bin/bash
# shebang(#!): tell script which interpret to use

# without a shebang, I need to invoke the interpreter manually
# bash script.sh
# defaults to usually, bin/sh (bourne)

# with a shebang, I can just execute
# ./script.sh



echo Hello World!
echo $SHELL
# bash에서 환경변수에 접근하는 법 $

